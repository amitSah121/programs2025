1) lexing
2) tokenization - token, lexem, literal, line 
3) better error messages ,
   like line {} character {} caused this error and
        error occured from line {} ....
4) scannar , returns all tokens , use functions like advance(actually advances the search), peek(only peaks the character), match(like peek but returns true if it matches the string forward) 
5) 